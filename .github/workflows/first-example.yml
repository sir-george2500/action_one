name: My first Workflows 

on: push 

jobs:
    first_jobs:
        runs-on: ubuntu-lastest
        steps:
        - name: checkout action
          uses: actions/checkout@v4

        - name: Welcome message
          run:  echo "Hello SirGeorge is your first actions"
        
        - name: List the file 
          run:  ls 

        - name: Read the list of file
          run: cat README.md
        